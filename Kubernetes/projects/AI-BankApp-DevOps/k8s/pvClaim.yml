kind: PersistentVolumeClaim
apiVersion: v1
metadata:
    name: mysql-pvc
    namespace: bank-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: manual
  resources:
    requests:
      storage: 5Gi
  selector: 
    matchLabels:
      app: mysql
